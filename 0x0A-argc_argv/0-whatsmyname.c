#include <stdio.h>

/**
 * main - prinst program name
 * @argc: agument count
 * @argv: argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
(void) agrc;

printf("%s\n", argv[0]);
return (0);
}
